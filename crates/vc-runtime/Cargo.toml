[package]
name = "vc-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
vc-core = { path = "../vc-core" }
vc-llm = { path = "../vc-llm" }
vc-storage = { path = "../vc-storage" }
uuid = { version = "1.8", features = ["v4"] }
